import { GraphNode } from '.';

// export class FunctionWrapperNode extends GraphNode {
//   constructor(calc) {
//     super();
//     this.calc = calc;
//     this.inputs = Array.from(new Array(calc.length)).map((_, idx) => String.fromCharCode(64 + idx));
//   }
// }
